$(document).ready(function () {
    Single = new slide("Single");
    //init has a function callback for Cards Functions eg: clickable cards
    Single.init();

    Multiple = new slide("Multiple");
    Multiple.init();
});